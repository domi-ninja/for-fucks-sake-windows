# Agent Instructions

- Keep README content limited to features that exist and how to use them.
- Include exactly one usage sample per documented feature.
- Do not add setup instructions, roadmap notes, implementation details, duplicate examples, or aspirational behavior to the README.
- When adding a CLI command, add the command module, wire dispatch/help consistently, and validate the command from outside the repository root with a relative path.