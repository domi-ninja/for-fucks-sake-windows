{
  "name": "ffs",
  "version": "0.1.0",
  "private": true,
  "description": "Small CommonJS CLI with subcommands.",
  "bin": {
    "ffs": "./bin/ffs.cjs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "ffs": "node ./bin/ffs.cjs",
    "test:cli": "node ./bin/ffs.cjs test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "koffi": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}